<div>
  <h3><%= @post.title %></h3>
  <p><%= @post.body %></p>
</div>

<div class="comments">
  <h4>Comments</h4>
  <%= render @comments %>
  <%= link_to 'Create Comments', new_post_comment_path(@post) %>
</div>

<%= link_to 'Back', posts_path %>
